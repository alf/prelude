;;; setup-yas.el --- yasnippet configuration
;;
;;; Commentary:

;; Some settings for using clojure mode and clojure in org babel.
;;
;;; Code:

(require 'prelude-packages)

(prelude-require-package 'yasnippet)

(yas-global-mode)

(provide 'setup-yas)

;;; setup-yas.el ends here
